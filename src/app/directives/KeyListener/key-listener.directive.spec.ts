import { KeyListenerDirective } from './key-listener.directive';

describe('KeyListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyListenerDirective();
    expect(directive).toBeTruthy();
  });
});
